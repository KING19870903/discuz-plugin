Global: 
    tool : build_submitter

Default: 
    profile : [dev,publish] 

Profiles:
    - profile: 
      name : dev
      env : cmc_standard
      command : sh build.sh
      release : true

    - profile: 
      name : publish
      env : cmc_standard
      command : sh build.sh
      release : true

